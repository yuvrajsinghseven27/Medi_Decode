# MediDecode Backend

FastAPI asynchronous backend powering AI-driven prescription parsing, drug safety analysis, and adherence scheduling.

## Getting Started

```bash
# Create venv and install dependencies
uv venv
uv pip install -e ".[dev]"

# Run dev server
uv run uvicorn app.main:app --reload --port 8000
```
